#
# DEFAULTS -- Default kernel configuration for FreeBSD/pc98
#

machine		pc98 i386
options		PC98

# config(8) also reads sys/i386/conf/DEFAULTS for the i386 CPU
# architecture.  Keep shared ISA, memory, I/O and ATPIC defaults there so
# that this file contains only PC-98-owned policy.
